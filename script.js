/* the globalVar variable is coming from the different file that is not module and the globalVar variable is defined in the globally in that file so you can access this variable after this file load */
console.log(globalVar)
